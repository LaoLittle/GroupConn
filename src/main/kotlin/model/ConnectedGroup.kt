package org.laolittle.plugin.groupconn.model

/*
data class ConnectedGroup(
    val group: Group,
   val target: Group
){
    operator fun get(input: Group) = when (input){
        group -> target
        else -> group
    }

    operator fun contains(input: Group) = when(input){
        group,target -> true
        else -> false
    }
}

 */